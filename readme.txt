There are no special instrucions aside from opening the solution and running the tests
- The Facebook test usually takes 2 seconds, but has taken as many as 20 on some page loads
- The user 6 test will always fail as the first name returned doesn't have the correct name
- The user 23 test will throw a WebException error that is caught and falls under the protocol error